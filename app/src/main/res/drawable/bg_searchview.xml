<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Blanco con 70% de opacidad (alpha = B3) -->
    <solid android:color="@color/pink_translucent"/>

    <corners android:radius="24dp"/>

    <stroke
        android:width="1dp"
        android:color="@color/pink_translucent"/> <!-- También con algo de transparencia -->
</shape>
